#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(asdf:load-system 'cl-lox)
(defpackage :ros.script.clox.3796815203
  (:use :cl :lox))
(in-package :ros.script.clox.3796815203)

(defun main (&rest argv) (apply lox::main argv))
;; (defun main (&rest argv)
;; (declare (ignorable argv)))
;;; vim: set ft=lisp lisp:
